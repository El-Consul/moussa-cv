# Moussa CV

Single-page interactive CV built with plain HTML â€” no dependencies, no build step.

## Files

```text
index.html   # Complete CV (content + styles)
robots.txt   # SEO
sitemap.xml  # SEO
```

## Usage

Open `index.html` in any browser, or serve it:

```bash
npx serve .
```

Print-friendly: use the browser's Print to PDF for an offline copy.
